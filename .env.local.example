# Copy this file to .env.local and fill in your own key — never commit .env.local.
# Get a free Access Key at https://unsplash.com/developers (create an app, "Demo" tier is enough).
# Without this set, the book editor's image search falls back to its curated stock grid.
UNSPLASH_ACCESS_KEY=
